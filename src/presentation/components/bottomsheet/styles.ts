import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  backDrop: {
    height: 4,
    width: 60,
    alignSelf: 'center',
  },
  sheet: {
    position: 'absolute',
    bottom: 0,
    left: 0,
    right: 0,
    padding: 16,
  },
});
